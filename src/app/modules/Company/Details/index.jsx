import { Container } from './styles';
import Card from '../../../common/components/Card';

const Details = () => {
  const x = 'a';

  return (
    <Container>
      <Card>
        a
      </Card>
    </Container>
  );
};

export default Details;
