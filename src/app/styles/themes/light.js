import { colors } from '../tokens/colors';
import { breakpoints } from '../tokens/breakpoints';

export const unit = 16;

export const theme = {
  title: 'light',

  unit,

  colors,

  shapes: {},

  breakpoints,
};
